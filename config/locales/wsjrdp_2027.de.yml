de:
  activerecord:
    models:

      ### GROUPS

      group/root:
        one: CMT
        other: CMT
      group/unit:
          one: Unit
          other: Units
      group/ist:
          one: IST
          other: ISTs


      ### ROLES

      group/root/admin:
        one: Admin
      group/root/leader:
        one: Leader
      group/root/member:
        one: CMT

      group/ist/leader:
        one: IST Manager
      group/ist/member:
        one: IST

      group/unit/manager:
        one: Unit Manager
      group/unit/leader:
        one: Unit Leader
      group/unit/member:
        one: Youth Participant