= field_set_tag do
  - [:email, :first_name, :last_name, :nickname].each do |a|
    - if entry.show_attr?(a)
      - if current_user && entry.send(a).present?
        = f.labeled(a, f.readonly_value(a))
      - else
        = f.labeled_input_field(a)